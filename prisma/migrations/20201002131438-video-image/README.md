# Migration `20201002131438-video-image`

This migration has been generated by wilson-pc at 10/2/2020, 9:14:38 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Imagen" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "format" TEXT NOT NULL,
PRIMARY KEY ("id")
)

CREATE TABLE "Video2" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "web" BOOLEAN NOT NULL DEFAULT false,
PRIMARY KEY ("id")
)

CREATE TABLE "Resolution" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "videoId" TEXT NOT NULL,

    FOREIGN KEY ("videoId") REFERENCES "Video2"("id") ON DELETE CASCADE ON UPDATE CASCADE,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Imagen.name_unique" ON "Imagen"("name")

CREATE UNIQUE INDEX "Video2.name_unique" ON "Video2"("name")

CREATE UNIQUE INDEX "Resolution.name_unique" ON "Resolution"("name")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200930050138-formato..20201002131438-video-image
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -69,4 +69,25 @@
   format String
   hls Boolean @default(false)
 }
+model Imagen{
+   id    String @id @default(uuid())
+  name  String @unique
+  format String
+ 
+}
+model Video2{
+   id    String @id @default(uuid())
+   name  String @unique
+  web Boolean @default(false)
+  resolutions Resolution[]
+
+}
+
+model Resolution{
+   id    String @id @default(uuid())
+   name  String @unique
+   videoId String
+   video Video2 @relation(fields: [videoId], references: [id])
+
+}
```


